# Geliştirme/test bağımlılıkları — ÜRETİME KURULMAZ.
#   venv/bin/pip install -r requirements-dev.txt
#   venv/bin/python -m pytest
-r requirements.txt
pytest>=8.0
pip-audit>=2.7        # bağımlılık zafiyet taraması: python -m pip_audit
