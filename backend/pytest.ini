[pytest]
testpaths = tests
markers =
    network: dış ağ erişimi gerektirir (DNS/TLS). `-m "not network"` ile atlanabilir.
filterwarnings =
    ignore:Parsed a serial number.*:DeprecationWarning
    ignore:Attribute's length.*:UserWarning
